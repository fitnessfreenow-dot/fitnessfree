const text="Welcome to Your Fitness Guide";let index=0;const speed=100;function typeEffect(){index<text.length&&(document.getElementById("typingText").innerHTML+=text.charAt(index),index++,setTimeout(typeEffect,speed))}const slides=document.querySelectorAll(".slide");let current=0;function showSlides(){slides.forEach((e,t)=>{e.classList.remove("active"),t===current&&e.classList.add("active")}),current=(current+1)%slides.length}function startSlider(){showSlides(),setInterval(showSlides,5e3)}function toggleMenu(){document.getElementById("navLinks").classList.toggle("active")}window.onload=()=>{typeEffect(),startSlider()};const fadeElements=document.querySelectorAll(".fade-in"),observer=new IntersectionObserver(e=>{e.forEach(e=>{e.isIntersecting&&e.target.classList.add("visible")})},{threshold:.1});fadeElements.forEach(e=>observer.observe(e));
